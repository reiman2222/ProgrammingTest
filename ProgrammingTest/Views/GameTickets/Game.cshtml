@model IEnumerable<ProgrammingTest.Models.GameTicket>

@{
    ViewData["Title"] = "Tickets by Game";
}

<h1>Tickets by Game</h1>

<p></p>

@Html.DropDownList("Locations", (SelectList)ViewBag.DropDownValues, "Select Game",
    new { @onchange = "CallChangefunc(this.value)" })

<p></p>
<script>
    function CallChangefunc(val) {
        window.location.href = "/GameTickets/Game?gameId=" + val;
    }
</script>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GameName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BlockDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TicketsPlayed)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.GameName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.BlockDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TicketsPlayed)
                </td>
            </tr>
        }
    </tbody>
</table>
